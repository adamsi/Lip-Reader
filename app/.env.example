# Optional override of the backend base URL. Leave unset:
#  - dev (npm run dev) falls back to http://localhost:8000 automatically
#  - production builds use relative URLs (same origin as the served SPA)
# Setting this bakes the URL into the production bundle - never point it at
# localhost for a deployed build.
# VITE_API_BASE=
